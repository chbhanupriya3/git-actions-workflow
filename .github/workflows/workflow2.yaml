name: Workflow2
on:
  workflow_run:
    workflows:
    -   workflow1 
    types:
    -   completed       

jobs:
    job1:
        if: ${{github.event.workflow_run.conclusion == 'success'}}
        runs-on: ubuntu-latest
        steps:
        -   name: Print details
            run: sleep 30