name: Workflow2
on:
  workflow_run:
    workflows:
    -   workflow1 
    types:
    -   completed       

jobs:
    job1:
        runs-on: ubuntu-latest
        steps:
        -   name: Print details
            run: sleep 30