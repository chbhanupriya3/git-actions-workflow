name: 01-env-variable
on:
   workflow_dispatch:
env:
    ENVIRONMENT: "dev"
    APP_NAME: i27Academy    
jobs:
    env-demo:
        runs-on: ubuntu-latest    
        steps:
        -   name: Print variables
            run: |    
                echo "Print environment variables"
                echo "Application name is: $APP_NAME"
                echo "Environment is: $ENVIRONMENT"
    
